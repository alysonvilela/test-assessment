version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./server:/app/server
      - /app/node_modules
    environment:
      NODE_ENV: development
    command: npm run dev:server

